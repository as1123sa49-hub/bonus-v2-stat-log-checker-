[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
addopts = -v -s --tb=short --strict-markers

markers =
    ui_comparison: UI comparison tests
    cocos: Tests requiring Cocos Creator

